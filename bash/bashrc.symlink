source ~/.dotfiles_config
files=$(find $DOTFILES/bash -type f ! -name '*.symlink'
for i in "${files[@]}" 
do
  source $i
done

# chruby
source /usr/local/share/chruby/chruby.sh

# load ssh-agent
if [ -z "$SSH_AUTH_SOCK" ] ; then
    eval `ssh-agent`
    ssh-add
fi

